{{> header}}

  <div class="hero" role="banner">
    <div class="hero__container">
      <h1>Accès à distance à la WebConférence de l'Etat</h1>
          <p>Audio, vidéo, chat, partage d'écran et de documents</p>
 
          {{#errors}}
            <div class="container">
              <div class="notification error">{{errors}}</div>
            </div>
          {{/errors}}
          {{#message}}
            <div class="container">
              <div class="notification">{{message}}</div>
            </div>
          {{/message}}
          {{^message}}
          <form action="/login" method="POST">
              <div class="form__group">
                <label for="email">Saissez votre email pour créer une conférence :</label>
                <input type="email" value="" name="email" id="email">
              
                <button class="button large">Recevoir un lien de conférence par email</button>
              </div>
          </form>
          {{/message}}
    </div>
  </div>
  <main role="main">
    <section class="section section-grey">
      <div class="container">
        <h2 class="section__title">A quoi sert ce service ?</h2>
        <div class="row">
          <p>Ce service permet de créer une <a href="https://webconf.numerique.gouv.fr/" target="_blank">webconférence de l'Etat</a> en dehors du Réseau Interministériel de l'Etat (RIE). </p>
          <p>Seules les adresses autorisées ont accès à ce service. Si votre adresse n'est pas autorisée, merci de faire une demande à <a href="mailto:webconf@beta.gouv.fr">webconf@beta.gouv.fr</a></p>
        </div>
      </div>
    </section>
  </main>
{{> footer}}
